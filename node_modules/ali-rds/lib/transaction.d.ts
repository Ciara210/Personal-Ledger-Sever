import type { RDSConnection } from 'ali-rds/lib/connection';
import { Operator } from 'ali-rds/lib/operator';
export declare class RDSTransaction extends Operator {
    #private;
    isCommit: boolean;
    isRollback: boolean;
    conn: RDSConnection | null;
    constructor(conn: RDSConnection);
    commit(): Promise<void>;
    rollback(): Promise<void>;
    protected _query(sql: string): Promise<any>;
}
